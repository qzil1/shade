# Shade - 需求文档

本文保留早期需求与技术设想；当前实现及验证范围以 `README.md` 和 `docs/release-readiness.md` 为准。

## 1. 项目背景

Shade 是一款 macOS 菜单栏专注工具，通过将非活动窗口变暗来减少视觉干扰。项目聚焦于核心遮罩功能的简洁、高效实现。

## 2. 核心需求

### 2.1 功能需求

| 优先级 | 功能 | 描述 |
|--------|------|------|
| P0 | 智能背景遮罩 | 自动识别当前焦点窗口，在非活动区域显示半透明黑色遮罩 |
| P0 | 多屏幕支持 | 支持多个显示器，每个屏幕独立计算遮罩区域 |
| P0 | 鼠标事件穿透 | 遮罩层不拦截任何鼠标点击或键盘事件 |
| P1 | 平滑动画 | 窗口焦点切换时，遮罩洞口以动画平滑移动（~200ms） |
| P1 | 菜单栏控制 | 菜单栏图标提供开关、强度调节、退出等功能 |
| P1 | 透明度调节 | 用户可调节遮罩不透明度（范围 0.1 ~ 0.9） |
| P2 | 快捷键支持 | 全局快捷键快速开关遮罩 |
| P2 | 应用白名单 | 允许为特定应用设置不同的遮罩行为 |
| P2 | 颜色自定义 | 支持自定义遮罩颜色（如夜间护眼暖色调） |

### 2.2 非功能需求

| 需求 | 描述 |
|------|------|
| 性能 | CPU 占用 < 1%，内存占用 < 50MB |
| 响应性 | 窗口焦点切换后 300ms 内完成遮罩更新 |
| 稳定性 | 不因系统睡眠/唤醒、屏幕插拔、Space 切换而崩溃 |
| 兼容性 | 支持 macOS 11.0+，Apple Silicon / Intel |

## 3. 技术方案

### 3.1 架构设计

```
┌─────────────────────────────────────────────┐
│              用户交互层                        │
│  ┌──────────────┐  ┌─────────────────────┐  │
│  │ 菜单栏控制器  │  │ 偏好设置面板 (Popover) │  │
│  └──────────────┘  └─────────────────────┘  │
├─────────────────────────────────────────────┤
│              业务逻辑层                        │
│  ┌──────────────┐  ┌─────────────────────┐  │
│  │ 遮罩管理器    │  │ 窗口监听器           │  │
│  │ OverlayManager│  │ WindowWatcher       │  │
│  └──────────────┘  └─────────────────────┘  │
├─────────────────────────────────────────────┤
│              渲染层                            │
│  ┌─────────────────────────────────────────┐ │
│  │ 遮罩窗口 (OverlayWindow + CAShapeLayer)  │ │
│  └─────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

### 3.2 关键技术决策

| 决策点 | 方案 | 理由 |
|--------|------|------|
| 遮罩实现 | CAShapeLayer + evenOdd 填充规则 | 性能高，支持动画，实现"挖洞"效果简洁 |
| 窗口位置获取 | macOS Accessibility API | 官方推荐方式，可获取精确的窗口 frame |
| 焦点监听 | NSWorkspace 通知 + 定时轮询 | 系统无原生"窗口移动"通知，轮询是必要补充 |
| 窗口层级 | NSWindow.Level.floating | 在普通窗口之上，系统 UI 之下 |
| 打包方式 | Swift Package Manager + build.sh | 轻量，无需 Xcode 工程文件 |

### 3.3 坐标系统

macOS 使用 Core Graphics 坐标系（原点左下角，Y 向上增长）。

- `NSScreen.frame`：全局坐标系中的屏幕位置
- `NSWindow.contentView.bounds`：本地坐标系，原点在左下角
- 转换公式：`localX = globalX - screen.minX`

## 4. 边界情况处理

| 场景 | 处理策略 |
|------|----------|
| 无焦点窗口（点击桌面） | 隐藏所有遮罩 |
| 应用自身为前台应用 | 跳过更新，保持上次遮罩状态 |
| 窗口跨屏幕 | 在相交的主屏幕上挖洞 |
| 屏幕插拔/分辨率变更 | 监听 `didChangeScreenParametersNotification`，重建遮罩窗口 |
| Mission Control / 全屏 | 遮罩窗口设置 `stationary` 和 `canJoinAllSpaces`，自动适应 |
| 未授权辅助功能 | 启动时自动弹出系统提示，未授权时遮罩功能静默失效 |

## 5. 扩展路线图

### V1.1（短期）
- [ ] 全局快捷键（启用/禁用、临时暂停）
- [ ] 自动启动选项
- [ ] 配置持久化（UserDefaults）

### V1.2（中期）
- [ ] 应用白名单/黑名单
- [ ] 遮罩颜色自定义
- [ ] 按显示器独立设置强度

### V2.0（长期）
- [ ] 与 macOS Focus 模式联动
- [ ] Shortcuts App 集成
- [ ] AppleScript / CLI 接口
